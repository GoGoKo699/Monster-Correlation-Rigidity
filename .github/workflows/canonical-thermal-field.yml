name: Canonical thermal field
on:
  push:
    branches: [research/canonical-thermal-field, main]
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    env:
      OPENBLAS_NUM_THREADS: '1'
      OMP_NUM_THREADS: '1'
      MKL_NUM_THREADS: '1'
      PYTHONDONTWRITEBYTECODE: '1'
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          ref: 09e3f98ad79034eefab43f4fb9ea432368fc19e7
          path: baseline
          persist-credentials: false
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          path: candidate
          persist-credentials: false
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.13.5'
      - run: python3 -m pip install 'numpy==2.3.5'
      - name: Frozen baseline and additive payload
        run: |
          python3 - <<'PY'
          import hashlib,json,pathlib,subprocess
          b,c=pathlib.Path('baseline'),pathlib.Path('candidate')
          names=subprocess.check_output(['git','-C',str(b),'ls-files','-z']).decode().split('\0')
          for name in filter(None,names):
              if (b/name).read_bytes()!=(c/name).read_bytes():
                  raise RuntimeError('Changed baseline file: '+name)
          m=json.loads((c/'results/canonical_thermal_field_additions.json').read_text())
          for e in m['files']:
              raw=(c/e['path']).read_bytes()
              if len(raw)!=e['bytes'] or hashlib.sha256(raw).hexdigest()!=e['sha256']:
                  raise RuntimeError('Addition hash mismatch: '+e['path'])
          print('BASELINE_PRESERVED and ADDITIONS_VERIFIED')
          PY
      - name: Strict replay and separate unchanged portability policy
        shell: bash
        run: |
          set -euo pipefail
          for tree in baseline candidate; do
            (
              cd "$tree"
              if python3 verify.py >/tmp/thermal-strict.out 2>/tmp/thermal-strict.err; then
                strict=PASS
              else
                strict=FAIL_RECOGNIZED_FLOAT_REPORT
                case "$(tail -n 1 /tmp/thermal-strict.err)" in
                  "RuntimeError: prior average toy differs from recorded report under None"|"RuntimeError: prior pair toy differs from recorded report under None") ;;
                  *) cat /tmp/thermal-strict.err; exit 1 ;;
                esac
              fi
              echo "STRICT_REPLAY $tree $strict"
              cat /tmp/thermal-strict.err
              python3 checks/replay_portability.py >/tmp/thermal-portability.json
              TREE="$tree" python3 - <<'PY'
          import json,os
          r=json.load(open('/tmp/thermal-portability.json'))
          print('PORTABILITY',os.environ['TREE'],r['status'])
          print('ALL_REPORTS_BYTE_IDENTICAL',os.environ['TREE'],r['all_reports_byte_identical'])
          print('DIFFERING_FLOAT_LEAVES',sum(x['different_float_leaves'] for x in r['reports']))
          PY
              test -z "$(git status --porcelain)"
            )
          done
      - name: Exact certificate in all three modes
        shell: bash
        run: |
          set -euo pipefail
          cd candidate
          python3 checks/verify_canonical_thermal_field.py >/tmp/thermal-normal.json
          python3 -O checks/verify_canonical_thermal_field.py >/tmp/thermal-O.json
          python3 -OO checks/verify_canonical_thermal_field.py >/tmp/thermal-OO.json
          cmp /tmp/thermal-normal.json /tmp/thermal-O.json
          cmp /tmp/thermal-normal.json /tmp/thermal-OO.json
          python3 - <<'PY'
          import hashlib,json,pathlib
          raw=pathlib.Path('/tmp/thermal-normal.json').read_bytes()
          expected=pathlib.Path('results/canonical_thermal_field_report.sha256').read_text().strip()
          if hashlib.sha256(raw).hexdigest()!=expected:
              raise RuntimeError('Exact report fingerprint differs')
          r=json.loads(raw)
          print('NEW_CHECKS',r['checks'],r['status'])
          print('CANONICAL_NORM',r['canonical_norm_squared'])
          print('COMPATIBILITY_NORM',r['crossed_channel_normalization']['normalization_recovered_from_thermal_compatibility'])
          print('REPORT_SHA256',expected)
          PY
          git diff --check
          test -z "$(git status --porcelain)"

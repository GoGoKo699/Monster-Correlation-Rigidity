name: Extended six-sevenths Ising criterion
on:
  push:
    branches: [research/extended-six-sevenths-ising, main]
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    env:
      OPENBLAS_NUM_THREADS: '1'
      OMP_NUM_THREADS: '1'
      PYTHONDONTWRITEBYTECODE: '1'
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          ref: 09e3f98ad79034eefab43f4fb9ea432368fc19e7
          path: baseline
          persist-credentials: false
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          path: candidate
          persist-credentials: false
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.13.5'
      - run: python3 -m pip install 'numpy==2.3.5'
      - name: Baseline preservation and additive hashes
        run: |
          python3 - <<'PY2'
          import hashlib,json,pathlib,subprocess
          b,c=pathlib.Path('baseline'),pathlib.Path('candidate')
          names=subprocess.check_output(['git','-C',str(b),'ls-files','-z']).decode().split('\0')
          for name in filter(None,names):
              if (b/name).read_bytes()!=(c/name).read_bytes():
                  raise RuntimeError('Changed baseline: '+name)
          for e in json.loads((c/'results/extended_six_sevenths_additions.json').read_text())['files']:
              raw=(c/e['path']).read_bytes()
              if len(raw)!=e['bytes'] or hashlib.sha256(raw).hexdigest()!=e['sha256']:
                  raise RuntimeError('Addition mismatch: '+e['path'])
          print('BASELINE_PRESERVED ADDITIONS_VERIFIED')
          PY2
      - name: Strict replay and separate unchanged portability
        shell: bash
        run: |
          set -euo pipefail
          for tree in baseline candidate; do
            (
              cd "$tree"
              if python3 verify.py >/tmp/extended-strict.out 2>/tmp/extended-strict.err; then
                strict=PASS
              else
                strict=FAIL_RECOGNIZED_FLOAT_REPORT
                case "$(tail -n 1 /tmp/extended-strict.err)" in
                  "RuntimeError: prior average toy differs from recorded report under None"|"RuntimeError: prior pair toy differs from recorded report under None") ;;
                  *) cat /tmp/extended-strict.err; exit 1 ;;
                esac
              fi
              echo "STRICT_REPLAY $tree $strict"
              cat /tmp/extended-strict.err
              python3 checks/replay_portability.py >/tmp/extended-portability.json
              TREE="$tree" python3 - <<'PY2'
          import json,os
          r=json.load(open('/tmp/extended-portability.json'))
          print('PORTABILITY',os.environ['TREE'],r['status'])
          print('ALL_REPORTS_BYTE_IDENTICAL',os.environ['TREE'],r['all_reports_byte_identical'])
          print('DIFFERING_FLOAT_LEAVES',sum(x['different_float_leaves'] for x in r['reports']))
          PY2
              test -z "$(git status --porcelain)"
            )
          done
      - name: Exact report in all three modes
        shell: bash
        run: |
          set -euo pipefail
          cd candidate
          python3 checks/verify_extended_six_sevenths.py >/tmp/extended-normal.json
          python3 -O checks/verify_extended_six_sevenths.py >/tmp/extended-O.json
          python3 -OO checks/verify_extended_six_sevenths.py >/tmp/extended-OO.json
          cmp /tmp/extended-normal.json /tmp/extended-O.json
          cmp /tmp/extended-normal.json /tmp/extended-OO.json
          python3 - <<'PY2'
          import hashlib,json,pathlib
          raw=pathlib.Path('/tmp/extended-normal.json').read_bytes()
          expected=pathlib.Path('results/extended_six_sevenths_report.sha256').read_text().strip()
          if hashlib.sha256(raw).hexdigest()!=expected:
              raise RuntimeError('Exact report fingerprint mismatch')
          r=json.loads(raw)
          print('NEW_CHECKS',r['checks'],r['status'])
          print('FORCED_RESPONSE_DIMENSION',r['extended_sector']['projector_trace'])
          print('TRIAL_AVERAGE',r['two_sign_trial']['average'])
          print('REPORT_SHA256',expected)
          PY2
          git diff --check
          test -z "$(git status --porcelain)"
